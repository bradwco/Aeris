--only-binary=:all:
# Use precompiled binary wheels where possible to avoid building numpy from source
# Pin a numpy minimum so pip will grab a compatible prebuilt wheel for your Python.
numpy>=1.24.0
opencv-python>=4.7.0
gpiozero>=1.6.2

# Optional but recommended for better servo control on Raspberry Pi:
# pigpio>=1.78 (install separately: sudo apt-get install pigpio python3-pigpio)
# Or: pip install pigpio (if available for your platform)

# Notes / fallback:
# - If pip fails stating a wheel is not available for your Python version, either:
#   * install via conda: `conda install numpy opencv -c conda-forge`, or
#   * download a matching prebuilt `.whl` for numpy from PyPI / a trusted wheel host
#     and install it first with `python -m pip install <numpy‑wheel-file>.whl` then
#     run `python -m pip install -r requirements.txt` (which will then use the binary
#     wheels already present).
